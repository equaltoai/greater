---
/**
 * Protected route wrapper that ensures authentication
 * This component doesn't redirect - that should be handled in the page itself
 */

// For now, just render the slot content
// Authentication check should be done at the page level
---

<slot />